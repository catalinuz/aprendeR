https://rextester.com/l/r_online_compiler

help(nls) # documentación sobre la palabra buscada
?nls
help.search("nls") # busqueda en el sistema
apropos("nls") # busca nombres que tienen algo que que ver
help.start() # abre la version HTML de la ayuda 
RSiteSearch("nls") # buscar solo en el http://search.r-project.org
abc <- c(1,2,3,4)

str(abc) # muestra la estructura interna del objeto
summary(abc) # muestra un resumen de los estadisticos más relevantes
class (abc) # muestra el tipo de dato

ls() # muestra todos los objetos del recurso 
ls(pat="b") # muestra los objetos que tienen el patron especificado ej: b 
ls.str() # por cada variable en cada recurso
dir() # ver los ficheros del directorio actual

getwd() # ver el directorio de trabajo 
setwd() # establecer/cambiar el directorio de trabajo
setwd(choose.file) # navegar hasta la carpeta

methods(abc) # List all available methods for a S3 and S4 generic function, or all methods for an S3 or S4 class
methods(class=class(abc)) # lists all the methods to handle objects of

